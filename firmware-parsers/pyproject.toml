[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "firmware-parsers"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []

[tool.maturin]
features = ["pyo3/extension-module"]
